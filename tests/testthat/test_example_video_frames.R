test_that("video frames", {
  # cmd <- paste(
  #   "Rscript ../../examples/video_frames.R",
  #   "--host", host,
  #   "--token", token,
  #   "--video_id", video_id,
  #   sep = " ")
  # exit_code <- system(cmd)
  # expect_equal(exit_code, 0)
})